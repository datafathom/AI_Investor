# Stabilization Cycle E: Social & AI/ML Services

## Goal
Stabilize the Social Trading and AI/ML component groups. These features drive user engagement and intelligent insights.

## Objectives
1.  **Audit**: Run existing tests for Social and AI/ML to establish a baseline.
2.  **Fix**: Resolve any failures, focusing on Pydantic models, data contracts, and missing method implementations.
3.  **Verify**: Ensure all tests pass.

## Scope

### Group E1: Social Trading
- `services/social/`
- `services/social_trading/` (if exists)
- `tests/social_trading/`

### Group E2: AI/ML
- `services/ai/` or `services/ml/` (to be located)
- `tests/api/test_ml_training_api.py`
- `tests/models/test_ml_training_models.py`

## Strategy
1.  **Initial Audit**:
    - Run `pytest tests/social_trading/`
    - Run `pytest tests/api/test_ml_training_api.py tests/models/test_ml_training_models.py`
2.  **Analysis**:
    - Check for `datetime` issues.
    - Check for Pydantic V2 compatibility.
    - Verify mock integrations for external APIs (Discord, Reddit, etc.) if any.
3.  **Remediation**:
    - Fix identified bugs.
    - Ensure robust error handling for external service calls.

## Verification
- Run the full "Group E" suite.
