# CLI Command: test-category

## Description
Run tests for a specific category (use test list to see all categories)

## Main Help Output
```text

Command: test-category
Description: Run tests for a specific category (use test list to see all categories)

Arguments:
  category             Test category name (e.g., backend-phase1, api-trading, models-core) (Required)

Flags:
  -v, --verbose            Verbose output (Default: False)
  --coverage           Generate coverage report (Default: False)
  -n, --parallel           Run tests in parallel (Default: False)
  -x, --fail-fast          Stop on first failure (Default: False)
  --html               Generate HTML test report (Default: False)


```

